apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "plumb.fullname" . }}-verifier
  labels:
    {{- include "plumb.labels" . | nindent 4 }}
data:
  verifier.yaml: |
    {{- toYaml .Values.verifier | nindent 4 }}
